version: '3'
services:
  graphdb:
    image: khaller/graphdb-free:10.2.1
    ports:
      - "7200:7200"
    volumes:
      - ./graphdb-data:/opt/graphdb/home
    networks:
      - pokemon-network

networks:
  pokemon-network:
    driver: bridge